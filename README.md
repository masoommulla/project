
  # ZEN-MIND AI Companion

  This is a code bundle for ZEN-MIND AI Companion. The original project is available at .

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  